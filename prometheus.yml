global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    project: "bibliotech"

scrape_configs:
  - job_name: "bibliotech-api"
    static_configs:
      - targets: ["api:3002"]
    metrics_path: /api/metrics
    scrape_interval: 15s
